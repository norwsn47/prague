## Design System

Before creating or modifying any UI component, read `docs/design-system.md` and follow it exactly. Every colour, font size, radius, spacing value, and component pattern must come from that file. Do not improvise or invent new tokens.
